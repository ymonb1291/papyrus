import { Level } from "./level.enum.ts";

export const DEFAULT_LEVEL: Level = Level.info;
export const DEFAULT_TIME: boolean = true;
export const LOG_VERSION: number = 1;
export const VERSION: string = "0.0.0";